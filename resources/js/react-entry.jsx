import { createRoot } from 'react-dom/client';

// React entry point. To mount a component, drop a
// <div data-react-root="my-widget"></div> in a blade view, add a
// components/MyWidget.jsx, and register it below:
//
// import MyWidget from './react/MyWidget.jsx';
// document.querySelectorAll('[data-react-root="my-widget"]')
//     .forEach((el) => createRoot(el).render(<MyWidget />));
